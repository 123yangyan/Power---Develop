---
description: 文档与 Rule 文件 UTF-8 编码规范，防止 .mdc/.md 中文乱码；编辑含中文的配置文件时必须遵守
alwaysApply: true
---

# 文档编码规范（杜绝乱码）

## 适用范围

以下路径含中文或必须长期可读，**一律 UTF-8（无 BOM）** 保存：

- `.cursor/rules/*.mdc`
- `.cursor/plans/*.plan.md`
- `.cursor/plans/*.md`
- `mindbody-android/FEATURE-LEDGER.md`
- `mindbody-android/PRODUCT.md`
- `mindbody-android/README.md`
- 根目录 `README.md`

---

## Agent 编辑规则（必须遵守）

1. **优先使用 Cursor 编辑器工具**（Write / StrReplace）修改上述文件，**不要**用 PowerShell `Set-Content`、`Out-File` 默认编码写入中文。
2. **禁止**在未指定 `-Encoding utf8` 的情况下用 shell 重写含中文的文本文件。
3. 若必须用脚本写入 UTF-8，仅允许：

```powershell
[System.IO.File]::WriteAllText($path, $content, [System.Text.UTF8Encoding]::new($false))
```

4. **编辑后自检**：读取文件 frontmatter 的 `description` 或首段标题；若出现 `???`、`?` 连续替换符或「心情」变成乱码，**立即用 Write 工具整文件重写 UTF-8**，不要提交乱码版本。
5. **禁止**用 `git add` 提交已确认乱码的 rule/plan 文件；先修复再 commit。

---

## 乱码常见原因（Windows）

| 错误做法 | 后果 |
|----------|------|
| `Set-Content file.mdc $text` 无 `-Encoding` | 按系统 ANSI/GBK 写，破坏 UTF-8 中文 |
| `Set-Content -NoNewline` 读 UTF-8 再写回 | 同上，且易丢换行 |
| 在 GBK 终端对 UTF-8 文件做字符串替换 | 部分字符变成 `?` |

---

## 脚本约定（本仓库）

- [`scripts/release.ps1`](../../scripts/release.ps1) 已使用 `UTF8Encoding(false)` 写 `VERSION` 与 gradle 文件；**保持此写法**。
- 新增 PowerShell 脚本若写中文或 markdown，必须使用同上 UTF-8 API，或只写 ASCII 路径/英文 message。

---

## 文件命名

Plan 与 Rule 文件名可含中文（如 `loop心情ai产品规划_9d502bd3.plan.md`）。**禁止**在 shell 脚本里用错误编码批量重命名这些文件。

---

## 恢复流程

发现 `.mdc` / `.md` 乱码时：

1. 从 Git 最后一次正常 commit 对比 diff，或按 [`feature-ledger.mdc`](feature-ledger.mdc) / 本文件同级规则的内容意图重写。
2. 用 Write 工具输出完整 UTF-8 正文。
3. 在 Cursor 中打开文件确认中文显示正常后再 push。

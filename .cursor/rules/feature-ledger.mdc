---
description: mindbody-android 已实现功能清单：执行前必读 FEATURE-LEDGER，新功能落地后追加条目
alwaysApply: true
---

# 功能实现清单（mindbody-android 专用）

权威清单：[`mindbody-android/FEATURE-LEDGER.md`](../../mindbody-android/FEATURE-LEDGER.md)

**范围**：仅 `mindbody-android/` 工程内**已实现**功能。不包含 server、app-main、emotion 参考库等。  
**待做特性**：只看 [`.cursor/plans/`](../plans/)，**不在清单中预填 pending 条目**。

与 [`project-priority.mdc`](project-priority.mdc) 分工：priority 管优先级与红线；本规则管 **mindbody-android 已落地功能的施工摘要**。

---

## 何时适用

- 用户任务涉及 `mindbody-android/` 代码 → **必须**读清单
- 用户任务仅涉及 server、其他目录 → **不**读、**不**写本清单

---

## 执行前（Agent 必须做）

1. 打开 `mindbody-android/FEATURE-LEDGER.md`，匹配已有功能 ID，读「已实现方案」，避免重复或破坏约定。
2. 若任务是新功能且清单无对应条目：**不要**预先添加 pending；改读对应 Plan 的 todo 与待实施设计。
3. 在回复开头写 `对齐清单：F-Px-xxx`（若匹配到已有条目）或 `对齐 Plan：todo-id`（新功能）。
4. 与已实现方案或架构红线冲突时，停手并说明冲突。

---

## 执行后（功能已落地时 Agent 必须做）

1. **追加或更新**清单条目（仅 mindbody-android 内已完成的代码）。
2. 写入「已实现方案」：入口、关键文件（相对 `com/owner/mindbody/`）、调用约定、验收要点。
3. 更新索引表与「变更记录」；更新「最后更新」日期。
4. 同步对应 Plan todo 为 `completed`。
5. 用户可见行为变化时提醒更新 `mindbody-android/PRODUCT.md`。

**禁止**：为未实现功能预填清单；只改代码不更新清单（完成功能后）。

---

## 新增功能 ID

`F-P{优先级}-{序号}`，例如 Phase 2 首条完成后为 `F-P2-001`。序号在同级优先级内递增。

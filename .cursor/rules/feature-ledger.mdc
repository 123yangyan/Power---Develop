---
description: 功能实现清单：执行前必读 FEATURE-LEDGER，执行后必须更新对应条目
alwaysApply: true
---

# 功能实现清单（执行前读 / 执行后写）

权威清单：[`.cursor/FEATURE-LEDGER.md`](../FEATURE-LEDGER.md)

与 [`project-priority.mdc`](project-priority.mdc) 分工：priority 管优先级与红线；**本规则管每个功能点的施工方案与真实现状**。

---

## 执行前（Agent 必须做）

1. **阅读清单**：打开 `FEATURE-LEDGER.md`，根据用户任务匹配功能 ID（如 F-P2-001）。
2. **无条目则新增**：在索引表与正文中按模板新增条目，状态 `pending`，再开始实现。
3. **读对应区块**：
   - `completed` → 读「已实现方案」，避免重复实现或破坏约定
   - `pending` / `in_progress` → 读「待实施方案」，对齐 Plan todo
4. **声明对齐**：在回复开头写 `对齐清单：F-Px-xxx`（可多 ID）。
5. **冲突停手**：与已实现方案或架构红线冲突时，列出冲突并等待用户确认。

---

## 执行后（Agent 必须做）

1. **更新条目状态**：`pending` → `in_progress` → `completed`（或 `cancelled` 并说明）。
2. **回写方案**：
   - 完成时：将「待实施方案」改写/补充为「已实现方案」（入口、关键文件、调用约定、验收要点）
   - 进行中：可只更新「变更记录」与状态
3. **变更记录**：追加一行 `YYYY-MM-DD：摘要 (#plan-todo-id)`
4. **更新索引表**：同步该 ID 的状态列与「最后更新」日期。
5. **同步 Plan**：对应 `.cursor/plans/*.plan.md` 的 todo status 一并更新。
6. **PRODUCT.md**：用户可见行为变化时提醒同步，不静默忽略漂移。

---

## 条目维护原则

- 一条功能 = 一个可独立验收的能力点（对齐 Plan todo 粒度）
- 「已实现方案」只写 Agent 下次施工需要的：入口、文件、约定、验收（≤40 行/条）
- 长设计留在 Plan；清单只保留施工摘要 + 真实现状
- **禁止**：只改代码不更新清单；清单与代码明显矛盾时不静默沿用过时描述

---

## 新增功能 ID 命名

`F-{优先级}-{序号}`，例如 `F-P2-001`。序号在同级优先级内递增。

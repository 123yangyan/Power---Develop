---
description: MindBody 项目优先级、Plan 对齐与变更记录约定（冲突时 Agent 必须遵守）
alwaysApply: true
---

# MindBody 项目优先级与变更治理

## 权威文档（按优先级）

1. **全局优先级与冲突规则** — 本文件
2. **mindbody-android 已实现功能清单** — `mindbody-android/FEATURE-LEDGER.md`（总索引）+ `mindbody-android/docs/feature-ledger/`（分清单；见 `feature-ledger.mdc`）
3. **产品真相源** — `mindbody-android/PRODUCT.md`
4. **路线图** — `.cursor/plans/loop心情ai产品规划_9d502bd3.plan.md`
5. **存储核心（横切模块）** — `.cursor/plans/统一存储核心模块_5f5a20f2.plan.md`
6. **代码实现** — Git 历史与 PR

若 PRODUCT.md 与 Plan 或代码不一致，以 **Plan + 已合并代码** 为准；发现漂移时先提醒用户同步 PRODUCT.md。

---

## 需求优先级（冲突时严格按此顺序）

| 优先级 | 范围 | 状态 |
|--------|------|------|
| **P0** | 统一存储核心 `data/storage`（AppStorage 门面、Room 迁移、SyncMeta、批量缓冲） | 已完成，维护性改动仍属 P0 |
| **P1** | Phase 1 稳定性：Polar BLE 连接、FTU、心率流、后台保活、`connectForSnapshot` | 已实现，bugfix 优先 |
| **P2** | Phase 2：emotion 坐标+日记+提醒+历史 UI 移植，本地关联 HR 快照 | pending |
| **P3** | Phase 3：server 扩展、HR+情绪融合 Pipeline、每日调度 | pending |
| **P4** | Phase 4：仪表页、7 日历史、manifest v6 指导展示 | pending |

**同优先级内**：先完成 Plan 中 `in_progress` 的 todo，再开新 todo。

**跨优先级冲突**：高优先级未完成或受威胁时，不得为了低优先级需求改动 P0/P1 的核心契约（见下文「架构红线」）。

---

## 架构红线（不可未经确认而破坏）

- 功能层（UI / ViewModel）**只通过** `MindBodyApplication.storage`（`AppStorage`）读写数据，禁止绕过门面直接访问 `AppDatabase` 或 Repository。
- 新数据类型接入存储的固定 4 步（Entity + SyncMeta → SyncableDao → Migration → AppStorage 暴露），不得跳步。
- Room：**禁止** `fallbackToDestructiveMigration`；schema 变更必须带显式 Migration。
- 心率样本：**禁止**恢复 24h 自动删除；`deleteOlderThan` 仅作将来手动归档，不得默认调用。
- 非医疗诊断：不做医疗级 HRV/压力诊断声明（见 PRODUCT.md 设计原则）。

---

## 每次改动前（Agent 必须做）

1. **读功能清单**（仅 mindbody-android 任务）：打开 `FEATURE-LEDGER.md` 总索引匹配 ID，再打开对应 `docs/feature-ledger/*.md` 分清单；新功能读 Plan，不预填清单。
2. **对齐 Plan**：说明对应 Plan todo id。若无对应项，先更新 Plan 再写代码。
3. **检查冲突**：与更高优先级或架构红线冲突时，**停止实现**，等待用户确认。
4. **控制范围**：最小 diff，不顺手重构无关模块。

## 每次改动后（Agent 必须做）

1. **功能已落地时**：更新对应 Phase 分清单 + `FEATURE-LEDGER.md` 总索引一行。
2. 将对应 Plan todo 标为 `completed`（或 `in_progress` / `cancelled`）。
3. 行为变化影响产品时，提醒更新 `PRODUCT.md`。
4. Git commit：`类型: 简述 (plan-todo-id)`。

---

## 依赖与阻塞关系

```
P0 存储核心 ──► P2 心情记录（mood 实体、HR 快照读存储）
P1 BLE/心率 ──► P2 connectForSnapshot 关联快照
P2 本地 mood ──► P3 上报与云端融合
P3 融合 Pipeline ──► P4 仪表与指导展示
```

blocked 的 Phase **不得**提前实现其消费者逻辑。

---

## Plan 文件索引

| Plan | 路径 | 状态 |
|------|------|------|
| 主路线图 | `.cursor/plans/loop心情ai产品规划_9d502bd3.plan.md` | 进行中 |
| 存储核心 P0 | `.cursor/plans/统一存储核心模块_5f5a20f2.plan.md` | 已完成 |
| Phase 2 心情记录 | `.cursor/plans/phase2心情记录移植.plan.md` | pending |
| Phase 3 云端融合 | `.cursor/plans/phase3云端融合.plan.md` | pending |
| Phase 4 仪表闭环 | `.cursor/plans/phase4仪表与闭环.plan.md` | pending |

**发版与推送**：Agent 聊天使用 `/release` 或 `/push` skill。

---

## Rule 文件索引（server 部署见 `server-ecs-deploy.mdc`）

| Rule | 路径 | 何时读 |
|------|------|--------|
| 项目优先级 | `.cursor/rules/project-priority.mdc` | 始终 |
| 功能清单 | `.cursor/rules/feature-ledger.mdc` | mindbody-android 任务 |
| 文档编码 | `.cursor/rules/doc-encoding.mdc` | 编辑含中文的 .mdc/.md |
| ECS 部署加速 | `.cursor/rules/server-ecs-deploy.mdc` | `server/` 部署、Docker、vitals API |

---

## 参考源码入口（mindbody-android）

- 应用入口：`MindBodyApplication.kt` → `storage: AppStorage`
- 存储门面：`data/storage/AppStorage.kt`
- BLE：`polar/PolarBleManager.kt`
- 已实现功能清单：`mindbody-android/FEATURE-LEDGER.md`（总索引）· 分清单 `docs/feature-ledger/`
